apiVersion: v1
kind: Service
metadata:
  name: {{ include "vuejs.fullname" . }}
  labels:
    {{- include "vuejs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vuejs.selectorLabels" . | nindent 4 }}
