apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vuejs.fullname" . }}-test-connection"
  labels:
    {{- include "vuejs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vuejs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
